# 🦀 Crab RPG Bot

Telegram-бот идлер-РПГ про краба, вдохновлённый War Crab: охота на морском дне,
прокачка уровня краба за золото, линька со сбросом уровня в обмен на очки ДНК,
покупка постоянных мутаций, копание камней-самоцветов, боссы-ивенты.

## Структура проекта

```
crab_rpg_bot/
├── bot.py              # точка входа, запуск polling + планировщик ивентов
├── config.py           # токен бота, ID админов, путь к БД (через переменные окружения)
├── data.py              # весь игровой контент: крабы, мутации, камни, магазин, мифические ивенты
├── game_logic.py        # формулы баланса (цены, урон, генерация монстров, ДНК за линьку)
├── events_logic.py       # честная взвешенная лотерея наград за ивенты
├── events_scheduler.py    # фоновый цикл: сам запускает/завершает мифические ивенты
├── database.py             # слой SQLite (таблицы users/stones/mutations/events)
├── keyboards.py             # reply-клавиатуры
├── states.py                 # FSM-состояния навигации + карта "куда ведёт Назад"
└── handlers/
    ├── start.py              # /start, обучение, выбор краба и берега
    ├── menu.py                # переходы между разделами + общая кнопка "Назад"
    ├── hunt.py                 # охота и бой тапами по "Атака" (+способности мутаций)
    ├── mutations.py             # линька + покупка/экипировка пассивных способностей
    ├── profile.py                # профиль, характеристики, ник, поиск игроков
    ├── shop.py                    # магазин за Telegram Stars (реальные платежи)
    ├── dig.py                      # копание камней (кулдаун 1 час)
    ├── inventory.py                 # инвентарь
    ├── craft.py                      # крафт ресурсов в Нектар силы
    ├── misc.py                       # топ игроков, помощь, статистика, ивенты/боссы
    ├── admin.py                       # /startboss, /endboss — ручное управление (опционально)
    └── middlewares.py                  # защита от падений при сбросе БД
```

## Что нового (двадцатый раунд — переезд на Amvera, способности, фиксы)

### 🔧 Переезд на Amvera Cloud
Полностью адаптировал под новый хостинг — детали и как именно устроена папка
`/data` см. в разделе "Деплой на Amvera Cloud" ниже.

### 🐛 Реальный баг max_meters — наконец работает
Ты добавил `max_meters` монстрам в `data.py`, но эффекта это не давало —
причина в том, что `game_logic.eligible_monsters()` фильтровала пул существ
**только** по `min_meters`, `max_meters` физически нигде не читался. Исправил
саму функцию (теперь учитывает оба поля) и заодно достроил `max_meters` **всем
25 видам** (у половины его вообще не было) — проверил тестом, что на любой
глубине от 0 и почти до бесконечности есть непрерывное покрытие без дыр, и
что мелкие виды (обычная рыба, рыба-камень) реально перестают попадаться на
большой глубине.

### 🎲 3 кнопки покупки мутаций → 1 случайная
Вместо отдельных "Купить/улучшить" на ноги/панцирь/клешни — одна кнопка
"🎲 Купить/улучшить мутацию": какая часть тела улучшится (или выпадет
впервые), решает случай при нажатии. Кнопки "Надеть/Снять" для уже открытых
артефактов остались отдельными, их объединять не просили.

### ⬅️ Кнопка "Назад" убрана из боя
Во время боя на нижней панели теперь только "🏃 Отступить" — раньше там
также была "Назад", хотя выйти из боя можно было только через отступление
(кнопка ничего не делала бы, кроме навигации, но во время боя это сбивало с
толку).

### ⚔️ Три активные боевые способности
Кнопки прямо на боевом сообщении, рядом с "Атака":
- **🛡️ Щит** (у всех) — блокирует 80% входящего урона в этом ходу, откат 3 хода.
- **🎯 Метка** (у всех, раз за бой) — двойное золото за помеченного врага, но
  следующие 2 твоих удара гарантированно мимо.
- **Уникальная способность** — своя у каждого краба, раз за бой:
  - Панцирник: **💥 Сокрушительный удар** — ×3 урона, но 15% своей прочности в отдачу.
  - Быстроход: **💨 Стремительный рывок** — 3 хода гарантированного попадания
    и уклонения, затем 4 хода усталости (чаще промахи, реже уклонения).
  - Крит-краб: **🩸 Кровавый раж** — до конца боя все удары критические, но
    каждый удар отбирает 6% прочности (при долгом использовании можно даже
    убить себя самого — это корректно обрабатывается тем же откатом позиции,
    что и смерть от врага).

Протестировал каждую способность отдельно и в связке: щит держит ровно 80%
блокировки на серии боёв, рывок даёт настоящее гарантированное уклонение (не
только попадание — это отдельно проверял и чинил), раж даёт крит на 100%
реальных попаданий, метка корректно удваивает золото. Отдельно нашёл и
исправил тонкий баг с меткой в засадах стражей: бонус золота случайно
применялся ко ВСЕЙ сумме за троих вместо одного помеченного стража — теперь
бонус точечно привязан к конкретному стражу и доживает до момента фактической
выплаты (в лагере она происходит только после зачистки всех троих).

## Что нового (восьмой раунд — полный аудит тестировщика)

По твоей просьбе прошёл ВЕСЬ бот как тестировщик: не вызовом функций напрямую
(это может маскировать баги роутинга), а через настоящий `aiogram.Dispatcher`
с полным стеком мидлварей — именно так, как реально работает Telegram. Нашёл
и исправил 4 реальных бага, два из которых — серьёзные.

### 🔴 КРИТИЧНО: весь магазин (звёзды + ракушки) был полностью нерабочим
В `mutations.py` фильтр колбэков был `F.data.startswith("buy_")` — слишком
широкий: он перехватывал `buy_star_golden_ray`, `buy_star_eternal_tide`,
`buy_shell_speed_dig` и т.д. РАНЬШЕ, чем они доходили до `shop.py`, потому что
`mutations.router` регистрируется в `bot.py` раньше `shop.router`. При нажатии
любой кнопки покупки не происходило вообще ничего — ни счёта, ни ошибки.
Плюс отдельно нашёл, что ключи в `SHELL_ITEMS` дублировали префикс `shell_` в
`callback_data`, что сломало бы магазин ракушек даже отдельно от первого бага.
Оба фикса подтверждены тестом с РЕАЛЬНО сгенерированными кнопками (а не
вручную написанным callback_data, как в моих более ранних тестах — именно
поэтому баг не всплывал раньше).

### 🟠 Кнопка "Назад" при смене ника уводила в главное меню вместо профиля
`Nav.waiting_nickname` не был прописан в `PARENT_STATE` — общий обработчик
"Назад" в `menu.py` (который выполняется раньше специального в `profile.py`)
подставлял дефолтное значение `Nav.main`. Добавил запись в карту, подтвердил
реальным тестом диспетчера.

### 🟡 Непоследовательная глубина навигации в разделе "Прочее"
"Топ"/"Помощь"/"Статистика" не переключали состояние (в отличие от "Ивентов"),
из-за чего "Назад" из них перепрыгивал сразу в "Меню", минуя список "Прочее".
Добавил состояние `Nav.misc_detail` — теперь все 4 пункта раздела ведут себя
одинаково.

### 🟠 Краш при ручном вводе "Назад" до выбора краба
Если написать текст "⬅️ Назад" руками на экране выбора краба (кнопки там нет,
но текст никто не мешает набрать) — общий обработчик уводил в главное меню
ДО того, как краб выбран, и следующее же игровое действие падало с
`KeyError: None` (характеристики пытались найти несуществующего краба).
Исправлено проверкой в самом начале `go_back`: если краб ещё не выбран,
переход просто игнорируется. Тоже касается случая, когда это происходит на
самом первом экране (`state=None`, до старта выбора краба).

### ✅ Что проверено и подтверждено рабочим (реальным диспетчером, не моками)
- Полная регистрация и онбординг с пагинацией обучения
- Вся охота: обычные бои, засады стражей (честный выбор цели, золото только
  за полную зачистку), отступление (не двигает позицию/HP), поражение
  (откат + полное лечение)
- Мутации: покупка (случайный артефакт), прокачка, экипировка/снятие, линька
  с подтверждением
- Профиль: смена ника (включая валидацию и попытку HTML-инъекции), поиск
  игрока, случайные игроки, магазин
- Копание с выбором длительности, сбор добычи, ресурсы
- Крафт нектара (включая нехватку ресурсов)
- Весь раздел "Прочее": топ, листаемая помощь, статистика, ивенты
- Полный флоу оплаты Stars: pre_checkout_query → successful_payment,
  включая начисление золота/ДНК и активацию Вечного прилива
- Ивенты/боссы целиком: `/startboss`, атака, `/endboss` с порогом участия и
  честной раздачей наград (протестировано на 4 игроках с разным уроном)
- Автопланировщик ивентов (автозапуск при отсутствии активного, автозавершение
  по истечении времени)
- Систематическая проверка ВСЕХ callback-фильтров и текстовых хендлеров на
  пересечения (нашёл и исправил вышеупомянутые; остальные пересечений не имеют)
- Полный сквозной прогон: регистрация → все экраны меню → назад из каждого →
  мутации → линька → "Прочее" со всеми подпунктами — без единого необработанного
  апдейта или исключения

### Честно о непроверенном
Не тестировал: поведение при реальной сетевой нестабильности Telegram (кроме
симуляции сбоя edit_message_text), поведение под реальной конкурентной
нагрузкой множества одновременных пользователей (только точечные гонки
состояний для одного пользователя), и визуальный вид ASCII-арта на разных
устройствах/шрифтах (не могу проверить рендеринг на реальном клиенте Telegram
из этой среды).

## Что нового (девятнадцатый раунд — окончательное решение: никаких текстовых тупиков)

Прошлый фикс убрал бесконечное зависание, но не саму причину, почему первое
нажатие могло "не сработать" — при частых повторных тапах (как на
скриншоте, 5 нажатий подряд за секунды) бот всё ещё иногда отвечал текстом
"уже сражаешься"/"чуть-чуть не успел" вместо реального боя, и приходилось
жать ещё раз.

Убрал эту причину полностью, переосмыслив логику: **"Рыскать по дну" больше
никогда не даёт текстовый тупик.** Если атомарная попытка начать новый бой
не удалась (уже в бою — неважно, из-за честной гонки с самим собой при
частых тапах, гонки с "Отступить", или что угодно ещё) — бот СРАЗУ
показывает АКТУАЛЬНЫЙ боевой экран с рабочей кнопкой "⚔️ Атака", а не
предлагает попробовать снова. То есть при любом раскладе — сколько раз
подряд ни жми "Рыскать по дну" — результат один: перед тобой живой монстр,
готовый к бою.

Заодно убрал и саму 0.2-секундную паузу-повтор из прошлого фикса — она была
не нужна и только чуть задерживала обычный (без гонки) случай.

Протестировано явно: 5 "одновременных" нажатий (и в свежем состоянии, и уже
находясь в бою, и вперемешку с "Отступить") — **ноль текстовых тупиков** в
каждом случае, всегда показывается рабочий бой. Плюс весь прежний набор
тестов (самовосстановление от сломанных состояний, устойчивость к сбою
сети, отсутствие дублирования монстров) по-прежнему проходит.

## Что нового (восемнадцатый раунд — исправлен бесконечный цикл "чуть-чуть не успел")

Скриншот показал серьёзный баг: мой прошлый фикс мог оставить игрока в
**бесконечном цикле** "Чуть-чуть не успел — попробуй ещё раз", навсегда.
Настоящая причина: старая защита от гонки была ДВУХШАГОВОЙ — сначала
атомарно "занять слот охоты", ПОТОМ отдельным запросом записать самого
монстра. Если МЕЖДУ этими двумя шагами что-то падало (сетевая заминка при
отправке сообщения, любой сбой) — слот оставался занят НАВСЕГДА без
монстра. А раз монстра нет, ни одна из последующих попыток не могла ни
показать "уже сражаешься" (для этого нужен монстр), ни занять слот заново
(он ведь уже занят) — получался тупик, воспроизводящийся на каждое
следующее нажатие.

Переделал фундаментально:
- Слот охоты и данные монстра теперь записываются **одним атомарным SQL-
  запросом** — либо всё вместе, либо ничего. Разбитого промежуточного
  состояния теперь не существует в принципе.
- Добавлено **самовосстановление**: если вдруг обнаруживается уже сломанное
  состояние (слот занят, а монстра нет — ровно то, что было на скриншоте),
  бот сам освобождает слот и тут же пробует снова, прозрачно для игрока.
- Если что-то падает уже ПОСЛЕ успешной записи монстра (например, при
  отправке сообщения) — слот немедленно освобождается, а не остаётся висеть.

Протестировал все сценарии явно: точную последовательность со скриншота,
имитацию уже сломанного состояния (самовосстановление сработало с первой
попытки), имитацию сетевого сбоя при отправке (слот корректно освобождается),
плюс весь прежний набор из 12+ проверок охоты — всё по-прежнему проходит.

## Что нового (семнадцатый раунд — баг "отступил, а поиск не работает")

Скриншот точно показал проблему: после "🏃 Отступить" следующие нажатия
"🔎 Рыскать по дну" получали "Ты уже сражаешься!", хотя бой уже закончился —
и на клавиатуре в этот момент не было кнопки, чтобы попробовать снова
(только Отступить/Назад), поэтому казалось, что бот застрял.

Разобрался в два захода:

1. **Порядок проверки был неправильным.** Код сначала читал `in_hunt` ОБЫЧНЫМ
   чтением, и только потом (если казалось, что не в бою) пытался атомарно
   занять слот охоты. Если "Отступить" обрабатывался рядом по времени, это
   первое чтение могло увидеть устаревшее in_hunt=1. Поменял порядок —
   теперь атомарная попытка всегда идёт первой, а сообщение "уже
   сражаешься" строится по свежайшим данным, а не по значению, прочитанному
   заранее.

2. **Более глубокая находка** — тестом с реальной параллельной обработкой
   (не просто одно нажатие за другим, а два по-настоящему одновременных
   действия) выяснилось, что дело ещё глубже: если "Отступить" и "Рыскать
   по дну" обрабатываются практически в один момент, попытка занять слот
   охоты может ЧЕСТНО не успеть — сама запись отступления в базу ещё
   физически не произошла. Это не "устаревшее чтение", а настоящая гонка
   двух действий. Добавил короткую повторную попытку (пауза ~0.2 сек, и
   только если первая попытка не удалась) — она даёт шанс отступлению
   "доехать" до базы, прежде чем окончательно решать, что бой действительно
   идёт.

Протестировал именно точную последовательность со скриншота и более
жёсткий параллельный сценарий — теперь после отступления игрок гарантированно
оказывается либо в новом бою, либо получает точный статус, а не застревает
без кнопки продолжить. Отдельно проверил: задержка в 0.2 сек добавляется
ТОЛЬКО в редком случае реальной гонки — обычный поиск как занимал ~15мс,
так и продолжает.

## Что нового (шестнадцатый раунд — проверка скорости и удобства интерфейса)

### ⚡ Скорость — честный вердикт
Замерил реальное время выполнения каждого действия (не "работает без ошибок",
а именно миллисекунды):
- Одиночные действия: 3-23 мс на любую команду (атака ~10мс, покупка мутации
  ~16мс, навигация по меню 2-5мс) — это очень быстро, в пределах погрешности
  для восприятия человеком.
- Проверил и нагрузку: 25 "одновременных" игроков по 12 действий каждый —
  честный пересчёт (не то, что я по ошибке насчитал в позапрошлом
  раунде) показал среднее **8.3 мс на одно обращение** — практически
  идентично одиночному режиму. Действия не тормозят друг друга по сути,
  просто выполняются строго по очереди (это нормально для одного процесса
  на одном потоке) — пропускная способность больше 100 действий в секунду.
- Честно: WAL-режим (прошлый раунд) не дал заметной разницы в реальном
  тесте с реальным ботом (сравнил напрямую — 170мс/действие на "delete" vs
  195мс/действие на WAL при той же нагрузке, разница в пределах шума
  измерения) — моя более ранняя оценка "на треть быстрее" была на
  упрощённом синтетическом бенчмарке потоков, который не отражает, как
  реально работает асинхронный Python-бот. WAL — разумная практика без
  минусов, просто не был тем прорывом, каким казался. **Итог: если
  что-то ощущается медленным на практике — это сеть/хостинг, не код.**

### 🖥️ Удобство интерфейса — 2 реальные находки
Прошёлся по каждому экрану бота, глядя на итоговый вид сообщений:

1. **Крафт дублировал одну и ту же информацию дважды** — сначала список
   "Твои ресурсы" (например "🐚 Осколок панциря: 0"), затем чуть ниже те же
   самые числа ещё раз в "Рецепте" ("🐚 Осколок панциря: 0/3"). Объединил в
   один список формата "есть/нужно" с наглядной галочкой ✅/▫️.
2. **"Вечный прилив" в магазине читался нелепо**: "Сейчас это ≈**постоянный
   буст +15% к золоту и ДНК навсегда**. Цена: 400 ⭐" — символ "≈"
   (приблизительно) предназначен для чисел, а тут подставлялось целое
   предложение. Убрал эту строку конкретно для этого товара (и в описании
   инвойса тоже) — теперь просто "Цена: 400 ⭐" без нелепой приставки.

Заодно заменил однотипную сухую подпись "Действие:" (повторялась на 4
экранах — Характеристики/Копать/Крафт/Ивенты) на более естественные
контекстные фразы ("Повысить уровень?", "Забрать добычу?", "Готов
скрафтить?", "Готов атаковать?").

## Что нового (пятнадцатый раунд — вероятная первопричина задержек: режим SQLite)

Раз задержка на "Рыскать по дну" повторилась снова уже ПОСЛЕ того, как я
исключил логические причины (спам сообщений, гонки состояний) — значит,
дело может быть глубже, на уровне самой базы данных. Проверил конфигурацию
SQLite и нашёл вероятную первопричину именно НЕПОСТОЯННЫХ задержек (то
работает быстро, то заметно тормозит):

- SQLite по умолчанию использует режим журнала `delete`, при котором ЛЮБАЯ
  запись блокирует вообще ВСЕ остальные обращения к базе (даже на чтение)
  на время этой записи.
- Бот открывает **новое соединение почти на каждый вызов** к базе (получить
  игрока, камни, мутации, атомарно занять слот охоты, записать результат —
  это 4-5 отдельных обращений на одно игровое действие).
- Python по умолчанию ждёт освобождения блокировки до 5 секунд, прежде чем
  выдать ошибку — то есть при столкновении обращений отдельные шаги могли
  реально зависать на секунды.

Включил WAL-режим (`PRAGMA journal_mode=WAL`) — это стандартное решение
именно для такого паттерна (много коротких соединений, частые записи):
читатели больше не блокируются писателями и наоборот, блокировки остаются
только между двумя одновременными ЗАПИСЯМИ, что гораздо реже. Плюс задал
явный `busy_timeout`, чтобы даже в редких случаях столкновения ожидание
было конечным и предсказуемым.

Проверил на прямом нагрузочном сравнении (15 параллельных потоков, каждый
делает 20 циклов чтение-запись — это грубая, но показательная имитация
того, как бот дёргает базу): обычный режим — 0.86 сек общего времени под
нагрузкой, WAL — 0.54 сек, то есть примерно на треть быстрее именно в
сценарии параллельных обращений. Заодно прогнал весь набор из 12 проверок
"Рысканья по дну" ещё раз — все по-прежнему проходят, ничего не сломалось.

## Что нового (четырнадцатый раунд — да, проверил, и нашёл новую регрессию)

По твоему вопросу перепроверил "Рыскать по дну" целиком — прогнал 12
конкретных проверок (одно нажатие = сразу монстр, гонка состояний, полный
бой инлайн-атакой, лагерь на 100м, восстановление потерянного боя и т.д.).
11 прошли сразу, но **одна вскрыла новую регрессию**, которую я сам внёс
прошлым раундом при объединении входа в охоту: если "Рыскать по дну"
приходит НЕСКОЛЬКО раз, пока бой уже идёт (что не должно происходить через
видимые кнопки интерфейса, но теоретически возможно), код каждый раз
пересылал ПОЛНЫЙ экран боя заново — не дублируя самого монстра, но создавая
ровно тот спам сообщений, который мы уже чинили раньше.

Поправил: теперь при "уже в бою" — короткий ответ без пересылки экрана,
и только в ДЕЙСТВИТЕЛЬНО сломанном случае (в базе бой есть, а сохранённого
ID боевого сообщения нет — например, после сбоя) экран воссоздаётся заново.
Перепроверил оба случая явными тестами — обычная гонка больше не спамит,
а по-настоящему потерянный бой всё ещё корректно восстанавливается.

## Что нового (тринадцатый раунд — атака на сообщении, стражи на 100м, чистка магазина)

### ⚔️ "Атака" перенесена с нижней панели на инлайн-кнопку сообщения
Раньше каждый тап по "Атака" отправлялся как обычное сообщение от твоего
имени (видно в истории чата справа) — при активном бое это реально
захламляло переписку. Теперь "⚔️ Атака" — инлайн-кнопка прямо на самом
боевом сообщении (`handlers/hunt.py`, callback `hunt_attack`), тап по ней
никаких новых сообщений не создаёт — тот же принцип, что уже был у выбора
цели в засаде стражей. Нижняя панель во время боя теперь показывает только
"🏃 Отступить" и "⬅️ Назад". Протестировано: серия атак через инлайн-кнопку
не создаёт ни одного нового сообщения, только правки существующего.

### 🛡️ Стражи теперь на 100 метрах вместо 50
Поменял `GUARD_CAMP_INTERVAL` в `data.py` и поправил текст в разделе Помощь,
чтобы не было рассинхрона с реальным поведением.

### 🎯 Заодно чиню выбор "не встреченных" стражей
Теперь, когда кнопка выбора цели и кнопка атаки — на одном и том же
сообщении (см. выше), это исключило путаницу, при которой раньше можно
было случайно бить только "текущего" по умолчанию противника, не замечая
кнопки выбора других двух. Протестировано явно: можно выбрать ЛЮБОГО из
трёх стражей в любом порядке, включая тех, кого ещё не атаковал.

### 🧹 Убрал технические тексты из магазина
Описания товаров звучали как объяснение формулы баланса ("Даёт золото,
равное ~9 уровням прокачки... на твоём текущем этапе игры") — это было
похоже на выдержку из документации, а не на игровой текст. Переписал на
простой человеческий язык ("Щедрая порция золота для быстрой прокачки
краба"), убрал фразу "Награда считается в процентах от твоего ТЕКУЩЕГО
прогресса" из шапки магазина (заменил на "Чем дальше ты продвинулся — тем
щедрее награда"). Заодно поправил опечатку "их даёшь" → "их дают" в блоке
про раковины наутилуса.

## Что нового (двенадцатый раунд — "Рыскать по дну" теперь работает с одного нажатия)

Отдельно разобрал именно вопрос "почему кажется, что кнопка не сразу
работает". Причина: у кнопки было ДВА разных поведения под одинаковым
текстом. Из главного меню первое нажатие только открывало экран охоты
(показывало позицию), а реальный поиск врага запускался лишь ПОВТОРНЫМ
нажатием той же самой на вид кнопки — раз текст не менялся, казалось, что
первое нажатие ничего не сделало.

Объединил в одно действие (`handlers/hunt.py → perform_search`,
переиспользуется и из главного меню, и с экрана охоты): теперь нажатие
"Рыскать по дну" из главного меню сразу ищет и показывает противника, без
промежуточного экрана. Заодно на всякий случай сделал so, что если бой
"потерялся" из-за сбоя (например, FSM-состояние сброшено, а в базе бой
всё ещё активен) — повторное нажатие покажет ТОТ ЖЕ бой, а не создаст
новый/не продублирует монстра. Протестировано: обычный монстр, лагерь
стражей и восстановление потерянного боя — все три сценария теперь
срабатывают за одно нажатие.

## Что нового (одиннадцатый раунд — гонка состояний при старте охоты)

Со скриншота стало видно: при нескольких быстрых нажатиях "Рыскать по дну"
подряд создавалось СРАЗУ ДВА разных монстра одновременно (Рыба-камень и
Медуза-парус в одном чате) — классическая гонка состояний, тот же класс
бага, что чинил раньше для трат золота/ДНК и сбора добычи, но здесь я
пропустил именно точку СТАРТА охоты. Функция читала `in_hunt` и только
потом (отдельным шагом) записывала `in_hunt=1` — между чтением и записью
могло проскочить ещё одно почти одновременное нажатие, которое тоже видело
"охота не начата" и спавнило свой отдельный монстр поверх первого.

Добавил атомарный захват слота охоты (`database.try_claim_hunt_slot`) —
теперь занять слот может ровно один запрос, все остальные, пришедшие почти
одновременно, корректно получают "Ты уже сражаешься!". Протестировал ровно
сценарий со скриншота — 6 "одновременных" нажатий "Рыскать по дну" теперь
создают ровно ОДНОГО монстра вместо нескольких.

## Что нового (десятый раунд — реальный источник спама в бою и слишком частые промахи)

### 🎯 Промахи были слишком частыми — ребаланс
Базовый шанс промаха игрока (6%) плюс "сложно попасть" у некоторых существ
(до 46%!) давали суммарный шанс промаха **до 52%** на отдельных монстрах —
больше половины ударов уходило в молоко. Изначально задумывалось, что
высокий шанс промаха будет редкой особенностью 1-2 существ (как угорь),
а получилось, что почти треть из 25 видов имела 25-46% уклонения. Пересчитал
всех существ: теперь максимум 22% суммарного промаха (у угря, как и
задумывалось изначально — самый "скользкий"), у большинства — 7-14%,
базовый шанс игрока снижен до 4%.

### 🐛 Реальная причина спама сообщений в бою — исправлено
Если и игрок, и монстр промахивались в одном раунде подряд несколько раз,
получившийся текст боевого экрана (HP-бары, лог) становился **побайтово
идентичным** предыдущему — а Telegram в таком случае отвечает ошибкой
"message is not modified" на попытку правки. Мой код ошибочно трактовал
ЛЮБУЮ ошибку правки как сбой и слал новое сообщение — при частых промахах
(что усугублялось прошлым пунктом) это создавало настоящий спам сообщений,
из-за которого боевое окно "уезжало" вверх и переставало быть видно.

Теперь это конкретное сообщение об ошибке распознаётся отдельно и
корректно трактуется как "всё в порядке, менять нечего" — новое сообщение
в этом случае не отправляется вообще. Протестировал напрямую: 15 атак
подряд с гарантированными промахами с обеих сторон теперь дают ровно 1
правку и 0 лишних сообщений (раньше в этом сценарии — 15 сообщений спама).
Обычный бой с реальным уроном по-прежнему обновляет экран на каждый тап
через правку — новые сообщения появляются только в моменты, где Telegram
физически не даёт по-другому (смена нижней клавиатуры при начале/конце боя).

## Что нового (девятый раунд — вернул синхронизацию нижней панели)

В седьмом раунде я убрал "лишнее" сообщение на 6 экранах (Характеристики,
Копать, Крафт, Мутации-магазин, Помощь, Ивенты), посчитав, что нижняя панель
и так уже видна с предыдущего экрана. Это было ошибкой: Telegram не даёт
одновременно и обновить нижнюю reply-клавиатуру, и прикрепить инлайн-кнопки
к одному сообщению — а я убрал именно то сообщение, которое обновляло
нижнюю панель. В итоге при переходе на любой из этих 6 экранов внизу
оставалась ВИДНА старая клавиатура (например, с "Инвентарь"/"Крафт" из
Меню), хотя эти кнопки уже не работали в новом состоянии — нажатия на них
просто тихо игнорировались. Кнопка "Назад" при этом работала, потому что
она глобальный обработчик и не привязана к конкретному состоянию.

Вернул обновление панели до одной кнопки "⬅️ Назад" на всех 6 экранах —
проверено: теперь при входе, например, в "Статистику" панель внизу реально
показывает ровно одну кнопку, старые нерабочие кнопки пропадают из виду, а
не просто перестают отвечать. Да, это возвращает 2 сообщения при первом
входе на экран (правильная синхронизация панели важнее, чем экономия одного
сообщения) — переключение между действиями внутри экрана (покупка мутации,
крафт, копание) по-прежнему редактирует одно и то же сообщение, не плодя
новых.

## Что нового (восьмой раунд — исчерпывающее тестирование по запросу "перепроверь всё")

По твоей просьбе прогнал бота как тестировщик: код-ревью каждого файла +
десятки тестов через **настоящий диспетчер aiogram** (не просто вызов функций
напрямую, а реальная симуляция апдейтов Telegram — это ловит баги в
маршрутизации, которые не видны при прямом тестировании функций).

### 🔴 КРИТИЧЕСКИЙ БАГ (уже исправлен): весь магазин был нерабочим
В `mutations.py` фильтр колбэков `F.data.startswith("buy_")` был слишком
широким и перехватывал `buy_star_...` (звёзды) и `buy_shell_...` (ракушки)
раньше, чем они доходили до `shop.py` — при нажатии ЛЮБОЙ кнопки покупки в
магазине ничего не происходило вообще, без единого сообщения об ошибке.
Плюс отдельно нашёл, что ключи в `SHELL_ITEMS` дублировали префикс `shell_`
в `callback_data`, что сломало бы магазин ракушек даже без первого бага.
Оба фикса подтверждены тестом с РЕАЛЬНО сгенерированными кнопками (не
руками написанным callback_data) — теперь звёздные покупки шлют инвойс,
покупки за ракушки корректно списывают ракушки и начисляют награду.

### 🟡 Найдено и исправлено: два бага навигации
- Кнопка "Назад" при отмене смены ника уводила в главное меню вместо
  профиля (не хватало записи в карте переходов).
- "Топ"/"Помощь"/"Статистика" в разделе "Прочее" не переключали состояние
  (в отличие от "Ивентов"), из-за чего "Назад" из них перепрыгивал сразу в
  "Меню", минуя список "Прочее" — несогласованно с остальной навигацией.
  Добавлено состояние `Nav.misc_detail` для единообразия.

### ✅ Проверено и подтверждено рабочим (без замечаний)
- Полный онбординг (регистрация, обучение с пагинацией, выбор краба).
- Полный боевой цикл: обычные бои, засады стражей (выбор цели, награда
  только за полную зачистку), поражение (откат+полное лечение), отступление
  (без изменений позиции/HP) — включая устойчивость к сбоям редактирования
  сообщений (принудительно смоделировал отказ Telegram API — бот всё равно
  ответил).
- Мутации: первая покупка (случайный артефакт), прокачка, экипировка/снятие,
  линька (блокировка по уровню, начисление ДНК, рост требований).
- Профиль: смена ника (включая защиту от HTML-инъекции и слишком короткого
  ввода), поиск игрока по нику (найден/не найден), случайные игроки,
  характеристики с апгрейдом уровня.
- Копание с выбором длительности, сбор добычи, крафт (успешный и с нехваткой
  ресурсов).
- Ивенты и боссы: `/startboss`/`/endboss` от админа (и корректное
  игнорирование от не-админа), проверка доступа по линькам/метрам/убийствам,
  честная раздача наград.
- Полный флоу оплаты Telegram Stars: `pre_checkout_query` → `successful_payment`
  → корректное начисление золота/ДНК/постоянного буста.
- Стресс-тест: ~250+ последовательных действий подряд (все экраны меню, все
  3 мутации, 21 цикл охоты) — ни одного необработанного исключения, финальная
  проверка целостности данных (золото/ДНК/HP/позиция/уровни мутаций — везде
  корректные неотрицательные значения).
- Систематически сверил ВСЕ callback-фильтры и текстовые фильтры кнопок по
  всему проекту на предмет пересечений между модулями (тот же класс багов,
  что нашёл в магазине) — других пересечений не найдено.

### Известные незначительные особенности (не баги, сознательно не трогал)
- Если игрок вручную наберёт текст кнопки, которая не отображается на
  текущей клавиатуре, бот промолчит (нет отдельного fallback-ответа
  "команда не распознана"). Реальные пользователи, нажимающие видимые
  кнопки, с этим не столкнутся.
- Старые инлайн-кнопки (например, "Повысить уровень" на сообщении, которое
  пользователь уже пролистал) остаются технически кликабельными даже после
  ухода с экрана — это стандартное поведение Telegram-ботов, не специфичный
  баг, и не создаёт некорректных состояний (действие всё равно применяется
  верно).

## Что нового (седьмой раунд правок — реальные баги интерфейса)

Три конкретных бага, которые ты заметил на реальном использовании:

### 🐛 Боевые "тормоза" — главный найденный баг
Я написал безопасную обёртку с `try/except` вокруг правки боевого сообщения,
но **забыл её реально подключить** — почти все вызовы `edit_message_text` шли
напрямую. Если Telegram отклонял правку (частые тапы → флуд-контроль,
сообщение устарело/удалено пользователем и т.п.) — хендлер падал с
необработанным исключением, и ты не получал вообще никакого ответа на тап
"Атака". Со стороны это выглядело как зависание/тормоза.

Исправлено: теперь **везде** в бою при неудачной правке бот сразу шлёт новое
сообщение вместо тишины (`handlers/hunt.py → _push_battle_update`).
Протестировано симуляцией: заставил `edit_message_text` ВСЕГДА выбрасывать
исключение — бой продолжил отвечать новыми сообщениями на каждый тап, ни
одного "молчания".

### 📖 Обучение в начале игры не листалось
Раньше стартовое обучение показывало только первую из 4 страниц без единой
кнопки пролистывания — в отличие от того же текста в разделе "Прочее →
Помощь", где пагинация работала. Теперь обучение при регистрации тоже
листается инлайн-кнопками ◀️/▶️ (`handlers/start.py`). Отдельно проверил, что
это не сломало пагинацию основной "Помощи" — они используют независимые
фильтры состояния (`StateFilter(None)` для онбординга), не конфликтуют.

### ✉️ Двойные сообщения — системная причина
Нашёл, что на 6 экранах (Помощь, Копать, Мутации, Крафт, Характеристики,
Ивенты) был один и тот же лишний паттерн: сначала отправлялось сообщение
ТОЛЬКО чтобы обновить нижнюю клавиатуру на "⬅️ Назад", а сразу следом —
второе сообщение с инлайн-кнопками действия. На деле нижняя клавиатура с
кнопкой "Назад" уже была видна с предыдущего экрана (например, из "Меню") —
пересылать её отдельным сообщением было не нужно. Сейчас все 6 экранов шлют
ровно одно сообщение с текстом и инлайн-кнопками сразу, полагаясь на уже
видимую клавиатуру для возврата назад. Протестировано — везде теперь ровно
одно сообщение на открытие экрана.

## Что нового (шестой раунд правок — убрал двойное наказание за смерть)

Хороший вопрос вскрыл реальную проблему: поражение наказывало ДВАЖДЫ —
откатом по дистанции И только 30%-м лечением с 2.5-часовым простоем до
полного восстановления. На малых дистанциях (десятки метров) это превращалось
в ловушку: откат съедал больше половины пути, а слабое HP означало скорую
повторную смерть или реальное многочасовое ожидание. Не то, что нужно
мобильной игре с короткими заходами в течение дня.

Исправлено:
- **Поражение теперь лечит ПОЛНОСТЬЮ** — единственная цена смерти это откат
  по дистанции, без дополнительного наказания слабостью.
- **Откат смягчён**: 12% текущей позиции, минимум 5м (было 15%, минимум 20м)
  — на 38м теряешь ~5м вместо гарантированных 20м.
- **Простойное лечение ускорено с 2.5 часов до 30 минут** до полного — теперь
  актуально только для добровольного отступления, а не для смерти.

Протестировано: смерть на 38м → откат до 33м, HP сразу максимум → можно тут
же снова пробовать без ожидания.

## Что нового (пятый раунд правок — визуал боя, 25 монстров, крафт, глубина вместо берега)

Это самый крупный раунд правок — почти полностью переписана боевая система.

### 🔎 Переименование и 25 уникальных существ
Кнопка "Искать врага" → **"🔎 Рыскать по дну"**. Вместо одного обобщённого
монстра теперь **25 уникальных видов** (`data.py → MONSTERS`), у каждого —
собственное соотношение HP/урона/золота и ASCII-арт:
- 🐍 Морской угорь — низкий урон, но **35% шанс, что твоя атака промахнётся**
  (тяжело попасть).
- 🪨 Рыба-камень — очень много HP, но мизерный урон.
- 💀 Стеклянный кошмар — критически мало HP (0.15×), но урон 3.2× — почти
  гарантированная смерть, если не добить быстро.
- Плюс ещё 22 вида с разными архетипами (осьминог-хитрец, акула-нянька,
  крабозавр, левиафанёнок и т.д.), которые появляются на разной глубине
  (`min_meters`) — чем дальше заходишь, тем экзотичнее противники.

### 🎲 Механика промаха (с обеих сторон)
Простая, без апгрейдов, как и просил:
- Игрок промахивается с базовым шансом 6% + индивидуальный "сложно попасть"
  показатель конкретного вида существа (`PLAYER_MISS_CHANCE` + `evasion` в
  `data.MONSTERS`).
- Существо промахивается по игроку через уже существующую характеристику
  уклонения (не менялась).

### 🖥️ Аккуратный боевой экран вместо простыни текста
Теперь один и тот же message.id **редактируется** на каждый тап "Атака"
(вместо отправки нового сообщения каждый раз) — в чате остаётся один опрятный
"боевой экран" с ASCII-артом врага, HP-барами (`█░`) и короткой строкой
последнего действия, а не длинная лента сообщений. Реализовано через
`bot.edit_message_text` с сохранением `battle_message_id` в БД.

### 📊 Характеристики обновляются "на месте"
При нажатии "Повысить уровень" теперь редактируется то же сообщение с
**полной** актуальной сводкой характеристик (было — только короткая строка) —
видно "изменено" вместо нового сообщения в чате.

### ⛏️ Копание с выбором длительности (без абуза)
Вместо фиксированного часа — выбор из 1/3/8/24 часов. Добыча камней и ресурсов
считается **строго линейно** от времени (`DIG_STONES_PER_HOUR`) — короткие
частые копания математически НЕ выгоднее одного длинного (проверено тестом:
копание 3ч дало ~9-10 камней, то есть точно пропорционально часу).

### 🏃 Отступление и поражение — честная переработка
Раньше отступление незаметно "продвигало" по дистанции и следующий бой лечил
полностью — по сути отступление ничего не стоило. Теперь:
- **Прочность НЕ восстанавливается автоматически** между боями — только
  медленно во время простоя вне охоты (полное восстановление ~2.5 часа
  простоя, `game_logic.apply_idle_regen`).
- **Позиция фиксируется ТОЛЬКО при победе** — отступление или поражение не
  засчитывают пройденные метры этой встречи.
- **Отступление** = честный "0": сохраняет текущую прочность и позицию как
  есть, наград нет. Настоящая цена — ты не продвинулся и не подлечился.
- **Поражение** = теперь настоящий откат: тебя отбрасывает на 15% текущей
  позиции назад (не на 0!), прочность восстанавливается частично (30%), а не
  полностью. Сообщение переформулировано под это.
Всё протестировано: отступление подтверждено НЕ двигающим позицию и НЕ
лечащим; поражение с позиции 200м откатило до 170м с частичным лечением.

### 🛡️ Засады стражей каждые 50 метров
Пересекая границу, кратную 50м (`GUARD_CAMP_INTERVAL`), вместо обычного
существа встречаешь **троих стражей** (усиленные версии обычных видов,
×1.25 к статам) — выбираешь, кого бить, через инлайн-кнопки. **Золото
начисляется только после полной зачистки всех троих** — протестировано:
зачистка лагеря из 3 стражей дала суммарное золото одним начислением, а не по
частям.

### 📖 Помощь разбита на страницы + объяснение характеристик
Вместо одной длинной простыни — 4 страницы с перелистыванием инлайн-кнопками
"◀️ Назад / Вперёд ▶️" (`data.HELP_PAGES`, `handlers/misc.py`). Добавлена
отдельная страница с объяснением каждой характеристики (урон/уклонение/
удача/крит. шанс/крит. урон/прочность).

### 🍯 Система крафта
Новые ресурсы (`data.RESOURCES`): 🐚 Осколок панциря, 💧 Морская эссенция,
🦴 Кость монстра — падают с убийств (15% шанс) и копания (шанс за каждый час).
В разделе "🍯 Крафт" (новый пункт меню) объединяешь 3 осколка + 2 эссенции +
1 кость в **Нектар силы** — временный бафф +25% к урону на 20 минут.
Протестировано: крафт корректно списывает именно нужные количества ресурсов,
бафф реально увеличивает урон в бою (проверено расчётом: 18.0 против 14.4
без баффа — ровно +25%).

### 🐚 Раковины наутилуса наконец при деле + новый донат
Раковины (награда с ивентов) теперь тратятся в магазине на:
- ⏩ Ускорить копание (мгновенно завершить текущее) — 3 🐚
- 🎁 Набор ресурсов для крафта (по 2 каждого) — 5 🐚

Новый донат за звёзды — **🌟 Вечный прилив** (400 ⭐): разовая покупка,
которая НАВСЕГДА даёт +15% к золоту и очкам ДНК со всех источников (охота,
линька, магазин). Протестировано: золото с бустом 121 против 105 без буста —
ровно +15%.

### 🌊 Берег убран — вместо него "уровень моря" по дистанции
Выбор берега при регистрации убран (сразу после выбора краба — в игру).
Вместо статичного берега — динамическая **зона глубины**, определяемая
рекордом пройденного пути (`data.DEPTH_ZONES`): 🏖️ Берег → 🌊 Мелководье →
🐟 Риф → 🌑 Сумеречная зона → ⚫ Бездна → 🔥 Адские глубины → 👑 Тронный зал
древних. Видна в собственном профиле, в профиле других игроков и в топе.

### 🔴 Камни — фикс жирного бафа (продолжение прошлого раунда)
Уже чинил это в четвёртом раунде, но раз тема снова всплыла — цифры
финализированы и перепроверены: 2 зелёных камня 1 уровня теперь дают +0.3%
удачи (было +2% — фикс держится, не откатился).

## Что нового (четвёртый раунд правок — аудит на абуз и баланс)

### 🐛 Камни в инвентаре — это не баг, а недостаток объяснения
Камни применяются к характеристикам **автоматически**, как только попадают в
инвентарь — их не нужно отдельно "использовать" или надевать (в отличие от
мутаций, которые надо покупать/надевать). Проверено тестом: добавление камня
сразу меняет результат `get_effective_stats`. Проблема была только в том, что
интерфейс не объяснял это — теперь в разделе «Копать» и в «Инвентаре» явно
написано, что камни уже активны.

### 🔒 Реальные уязвимости — гонки состояний при быстром двойном клике
При параллельной обработке апдейтов (aiogram по умолчанию не сериализует
обработку строго последовательно) быстрый двойной тап по одной и той же
кнопке мог привести к races: оба клика читают один и тот же баланс/состояние
до того, как первый успевает записать результат. Нашёл и закрыл 4 таких дыры,
переведя критичные операции на атомарные SQL-запросы (`database.py`:
`try_spend`, `try_start_dig`, `try_collect_dig`, `try_apply_attack_result`):
- Покупка/улучшение мутации — раньше можно было задвоить трату очков ДНК.
- Повышение уровня краба — то же самое с золотом.
- Копание — двойной клик "Забрать добычу" мог удвоить лут.
- Атака в бою — двойной клик мог дважды засчитать одну победу над монстром
  (двойное золото за одного убитого врага).

Каждый случай протестирован явной симуляцией "два клика почти одновременно" —
во всех четырёх местах теперь проходит РОВНО одно действие, второе честно
отклоняется. Смотри тестовый вывод выше в истории правок.

### 🔒 HTML-инъекция через никнейм
Ник показывается другим игрокам (топ, поиск, просмотр профиля) в сообщениях с
`parse_mode=HTML`. Без валидации игрок мог вписать `<b>`, `<a href=...>` и
подобное, что либо ломало чужие сообщения, либо потенциально использовалось
для фишинга внутри бота. Закрыл:
- Смена ника теперь разрешает только буквы (кириллица/латиница), цифры,
  пробел, `_` и `-` — остальное отклоняется с понятным объяснением.
- Ник по умолчанию при регистрации (`/start`) берётся из имени в Telegram
  (`username`/`first_name`) — а это ПРОИЗВОЛЬНЫЙ текст пользователя, не
  проверенный на безопасность. Теперь он тоже проходит ту же проверку, а если
  не проходит — подставляется безопасное generic-имя вроде `Краб42`.

### ⚖️ Реальный дисбаланс: ДНК не поспевало за ценой мутаций
Прогнав симуляцию на 8 линек подряд (с выходом на +20 уровней сверх минимума
каждый раз — то есть очень старательная игра), обнаружил: цена мутаций растёт
намного быстрее, чем доход ДНК. Итог до правки — на прокачку ОДНОГО артефакта
до 5 уровня уходило 93% всего ДНК, заработанного за 8 линек — то есть на все
остальные мутации не оставалось практически ничего, несмотря на десятки часов
игры. Это не абуз, а обратная проблема — система ощущалась бы почти
бесполезной. Пересчитал обе формулы, чтобы доход ДНК рос экспоненциально
вместе с ценой (`game_logic.dna_points_for_molt`, `mutation_cost`).
После правки: за те же 8 линек (737 ДНК) хватает ровно на прокачку **всех
трёх** артефактов до 5 уровня каждый (729 ДНК) — заметный прогресс без того,
чтобы всё сразу открывалось.

### 🎨 Эмодзи — почему у золотого ската была иконка рыбы (и что стало)
В Unicode нет отдельного эмодзи для ската/рыбы-ската — пришлось выбирать
компромисс. Вместо случайной "рыбной" иконки поставил эмодзи, которые прямо
показывают ТИП награды (так магазин сканируется глазами быстрее):
- 🥇 Золотой скат (даёт золото — медаль/премиум ассоциация вместо рыбы).
- 🧬 Синяя креветка (даёт ДНК — эмодзи спирали ДНК вместо креветки, хотя
  🦐 сам по себе был корректным emoji для креветки, менять пришлось ради
  единого принципа "эмодзи = тип награды").

Заодно поправил ещё 2 мелких нестыковки:
- Жемчужный панцирь: было ⚪ (просто белый кружок) → стало 🦪 (устрица,
  прямая ассоциация с жемчугом).
- Губчатый панцирь (было "Коралловый панцирь"): у ног уже был коралловый
  вариант с 🪸, и панцирь с той же иконкой визуально путался бы в списке —
  сменил материал на губку 🧽, чтобы все 9 артефактов были визуально различимы.

### 🖥️ Интерфейс — где раньше копились лишние сообщения
Нашёл одно место, где чат реально захламлялся: при многократном нажатии
"Повысить уровень" бот раньше слал НОВОЕ сообщение с кнопкой на каждый клик,
вместо того чтобы обновлять одно и то же. Поправил — теперь это (как и покупка
мутаций, которая уже была сделана правильно) редактирует одно сообщение на
месте. Остальные места, где отправляются новые сообщения (навигация по меню,
инвойсы за звёзды, боевой лог в охоте, просмотр чужого профиля), — это
осознанный выбор: навигация физически требует нового сообщения для смены
reply-клавиатуры, инвойсы Telegram нельзя "вклеить" в существующее сообщение,
а боевой лог — это осмысленная историческая лента событий боя, а не спам.

## Что нового (третий раунд правок)

### 💰 Магазин — награда в процентах от прогресса, не абузится
Раньше золотой скат/креветка давали фиксированное число — неправильно скошено
в пользу либо ранней, либо поздней игры. Теперь награда считается формулой
от ТЕКУЩЕГО состояния игрока (`game_logic.shop_gold_reward` /
`shop_dna_reward`):
- 🐟 Золотой скат (100 ⭐) — золото, равное цене ~9 уровней прокачки краба
  *на твоём текущем этапе* (использует ту же формулу `level_up_cost`, что и
  обычная прокачка).
- 🦐 Синяя креветка (150 ⭐) — очки ДНК, равные ~4 средним апгрейдам мутаций
  *на твоём текущем этапе* (использует `mutation_cost`).

Протестировано: на 20 уровне скат даёт 3078 золота, а на 90-м уровне (после
2 линек) — уже 42255 золота за ту же цену в звёздах. Абузить низкой ценой
на старте бессмысленно — там и цена уровней сама по себе крошечная, награда
масштабируется вместе с прогрессом.

### 🧬 Мутации — теперь настоящие артефакты со случайными статами
Как ты и уточнил — это не пассивные способности с шансом срабатывания, а
артефакты с фиксированным плюсом и минусом к характеристикам:
- Покупаешь мутацию для слота (ноги/панцирь/клешни) впервые — **выпадает
  случайный вариант** из пула (`data.py → MUTATION_VARIANTS`), например
  «🪨 Каменная нога» (+прочность, −уклонение) или «💧 Ртутная нога»
  (+уклонение, −урон).
- Дальнейшие покупки для этого же слота **прокачивают уже выпавший
  вариант** — усиливают и плюс, и минус пропорционально уровню.
- В каждом слоте 3 возможных варианта с разными комбинациями плюс/минус —
  для полного списка смотри `MUTATION_VARIANTS` в `data.py`, там же легко
  добавить свои варианты или свои значения.
- Протестировано: артефакт «Ртутная нога» уровня 1 даёт +3 уклонения и −1 к
  урону, на уровне 5 — уже +15 уклонения и −5 к урону (масштабируется линейно
  с уровнем).

### 🐉 Открытие ивентов — три альтернативных пути вместо жёсткой планки
5 линек оказалось слишком далёкой и однобокой целью — учитывая, что порог
линьки растёт на +15 уровней с каждым разом (к 5-й линьке это уже 130
уровень краба, реально много часов гринда только этим одним способом).
Теперь мифические ивенты открываются, если выполнено **любое** из условий
(`game_logic.mythic_events_unlocked`):
- 3-я линька, ИЛИ
- 2500 м. рекорд по пройденному пути, ИЛИ
- 600 убитых существ.

Это даёт разным стилям игры (линяльщик / глубинный ныряльщик / фармер мобов)
свой путь к ивентам, вместо запирания контента только за самым медленным из
трёх. Пороги настраиваются в `data.py` (`MYTHIC_EVENT_UNLOCK_*`).

## Что нового (второй раунд правок)

### 💰 Реальные платежи — Telegram Stars
Магазин (Профиль → 🛍️ Магазин) теперь принимает настоящую оплату **звёздами
Telegram** — это официальная встроенная валюта, отдельного юрлица/ИП не нужно,
работает "из коробки" через `send_invoice` с `currency="XTR"`. Актуальные
цены и формула расчёта награды — см. раздел "Что нового (третий раунд правок)"
выше, там же итоговая логика после правок по проценту от прогресса.
Полученные звёзды Telegram зачисляет на баланс бота в Fragment/Telegram —
вывод и конвертация делаются через официальный интерфейс Telegram для
владельцев ботов (Bot Settings → Stars/Balance в вашем аккаунте BotFather).

### 🐉 Мифические ивенты — теперь полностью автоматические
Больше не нужно руками запускать ивенты. `events_scheduler.py` работает в
фоне: каждые 30 минут проверяет, не пора ли завершить текущий ивент и не
запустить ли новый. Ивенты длятся по 48 часов и выбираются случайно из списка
в `data.py → MYTHIC_EVENTS` (6 тематических боссов с описаниями — свободно
дополняй список своими).

Актуальное условие открытия ивентов (три альтернативных пути, а не жёсткая
планка в 5 линек) — см. раздел "Что нового (третий раунд правок)" выше.

### 🎲 Честная раздача наград за ивенты (не только топу)
Раньше награды шли строго топ-20 по урону — теперь это взвешенная лотерея:
- Порог участия: нужно нанести хотя бы 30 суммарного урона (отсекает случайный
  клик мимо, но не более того).
- Все, кто прошёл порог, получают гарантированную базовую награду (ракушки +
  камень) — независимо от места в топе.
- Жемчужные кейсы с уникальными мутациями разыгрываются между ~35% участников
  через лотерею с весом **√урон** — это специально сглаживает разрыв между
  топом и рядовыми активными игроками, чтобы шанс получить редкую мутацию был
  не только у задротов с максимальным уроном. Протестировано: в тестовом
  прогоне с 12 участниками кейсы выпали и топовому игроку (5000 урона), и
  игрокам в середине и даже почти в конце списка (150 и 60 урона).

### 🧬 Уникальные мутации (из ивентов) реально работают в бою
Актуальную систему обычных мутаций (артефакты со статами) см. в разделе
"Что нового (третий раунд правок)" выше. Отдельные уникальные мутации из
жемчужных кейсов ивентов — это активные пассивные навыки, задействованные
прямо в бою (`handlers/hunt.py`):
- ☠️ **Яд** — 25% шанс отравить врага на 3 хода дополнительного урона.
- 🗡️ **Прокол** — 15% шанс на гарантированный крит.
- 🩸 **Вампиризм** — 15% нанесённого урона превращается в восстановленную прочность.
- 🌊 **Маскировка** — 20% шанс полностью избежать ответного удара (сверх обычного уклонения).
- 🪙 **Жадность** — 20% шанс удвоить золото за победу.
- 🌀 **Бешенство** — после крита 30% шанс на мгновенный дополнительный удар.

Проверено тестами (60 тапов подряд с крит-крабом) — все шесть эффектов
реально срабатывают в бою.

### ⚖️ Линька теперь вынуждает расти выше минимума
- Каждая следующая линька требует **более высокого уровня**, чем предыдущая:
  1-я — 70 уровень, 2-я — 85, 3-я — 100, и так далее (+15 за каждую).
- Очков ДНК даётся тем больше, чем **выше минимума** ты поднялся перед линькой
  — специально, чтобы не было смысла линять "впритык", а хотелось разогнаться
  повыше ради лишних очков на подорожавшие мутации.

### 🔍 Поиск других игроков
В профиле теперь две новые кнопки:
- **🔍 Найти игрока** — ищет по точному нику (без учёта регистра).
- **🎲 Другие игроки** — предлагает несколько случайных игроков с кнопками,
  по нажатию открывающими их публичный профиль (уровень, линьки, мутации,
  рекорд по метрам, убийства) — без приватных данных вроде золота/инвентаря.

## Запуск локально

```bash
pip install -r requirements.txt
export BOT_TOKEN="токен_от_BotFather"
export ADMIN_IDS="твой_telegram_id"      # можно несколько через запятую
python bot.py
```

Свой Telegram ID можно узнать, написав боту @userinfobot.

## Деплой на Amvera Cloud

Переехали на [Amvera](https://cloud.amvera.ru) — российский хостинг. Что для этого
сделано в проекте:

- **`amvera.yml`** в корне — конфиг сборки/запуска (окружение Python 3.12,
  `requirements.txt`, точка входа `bot.py`, постоянное хранилище `/data`).
- **`config.py`** — `DB_PATH` теперь по умолчанию `/data/crab_game.db`
  (переопределяется переменной окружения `DB_PATH`, если нужно другое имя).
- **`database.py`** — при старте сам создаёт директорию для файла БД, если её
  ещё нет (`os.makedirs`), так что даже при нестандартном `DB_PATH` бот не
  упадёт с "unable to open database file".

### Как это развернуть

1. **Создай проект** на [cloud.amvera.ru](https://cloud.amvera.ru), привяжи к
   нему этот git-репозиторий (через `git remote add amvera ...` и `git push`,
   либо через встроенный интерфейс — оба способа описаны в личном кабинете
   Amvera при создании проекта).

2. **Переменные окружения** — в разделе проекта "Переменные и секреты" добавь:
   - `BOT_TOKEN` — токен от @BotFather (отметь как секрет).
   - `ADMIN_IDS` — твой Telegram ID (через запятую, если админов несколько).

3. **⚠️ Про папку `/data` (отвечаю отдельно, раз спрашивал):** её не нужно
   создавать руками через код или git — это **отдельная сущность интерфейса
   Amvera**, не обычная папка репозитория. У проекта в личном кабинете есть
   раздел **"Данные" / "Data"** — именно это постоянное хранилище, которое
   Amvera на этапе запуска монтирует внутрь контейнера по пути, указанному в
   `run.persistenceMount` в `amvera.yml` (у нас это `/data`). Всё, что бот
   сохраняет по пути `/data/...` (у нас — файл базы `crab_game.db`), переживёт
   пересборки и рестарты. Ничего вручную создавать не требуется — просто
   убедись, что `amvera.yml` есть в репозитории (он там уже есть), и при
   первом запуске Amvera сама смонтирует `/data`, а SQLite создаст файл базы
   внутри неё при первом обращении.
   Важно: не путай папку `Data` (постоянное хранилище, живёт вне пересборок) с
   папкой `Code`/`Artifacts` (это твой репозиторий — он пересоздаётся при
   каждом деплое, всё несохранённое там теряется).

4. **Порт/контейнер** — бот работает через long-polling (сам стучится в
   Telegram, а не наоборот), поэтому HTTP-порт ему не нужен — в `amvera.yml`
   специально не указан `containerPort`.

5. **WAL-файлы БД** — как и раньше, рядом с `crab_game.db` появятся
   `crab_game.db-wal` и `crab_game.db-shm` — это часть базы, а не мусор, они
   тоже будут лежать в `/data` автоматически.

## Деплой на Railway (старый вариант, если решишь вернуться)

1. Залей этот код в GitHub-репозиторий.
2. На railway.app создай новый проект → Deploy from GitHub repo.
3. Во вкладке **Variables** добавь:
   - `BOT_TOKEN` — токен от @BotFather
   - `ADMIN_IDS` — твой Telegram ID (и других админов через запятую)
4. Railway сам подхватит `Procfile` (`worker: python bot.py`) — важно, чтобы тип
   процесса был именно **worker**, а не web (боту не нужен HTTP-порт).
5. **Про бесплатный тариф**: у Railway ограниченный бесплатный кредит в месяц.
   Небольшому боту с редкими игроками должно хватать, но при заметном онлайне
   стоит перейти на платный план (~$5/мес), иначе бот может "засыпать" при
   исчерпании кредита.
6. **⚠️ ОБЯЗАТЕЛЬНО подключи Railway Volume, иначе прогресс игроков будет
   стираться при каждом падении/перезапуске/редеплое бота:**
   - В проекте на Railway открой вкладку сервиса → **Settings → Volumes**
     (или кнопка "+ New" → Volume, в зависимости от версии интерфейса).
   - Создай volume, например с mount path `/data`.
   - Во вкладке **Variables** добавь переменную `DB_PATH` со значением
     `/data/crab_game.db` (без этого бот по умолчанию пишет базу в
     эпемерную папку контейнера, которая обнуляется при каждом рестарте).
   - Передеплой — теперь база будет жить на постоянном диске и переживёт
     любые падения/обновления кода.

   Без этого шага именно из-за сброса базы данных при рестартах Railway
   игроки будут время от времени видеть сообщение "прогресс был сброшен,
   нажми /start" (я добавил защиту в `handlers/middlewares.py`, чтобы бот хотя
   бы не падал в такой ситуации, но подключение Volume — единственный способ
   реально сохранять прогресс между рестартами).

   ⚠️ **Важно про WAL-режим:** база теперь работает в режиме WAL (см. раздел
   про производительность ниже) — это значит, что рядом с `crab_game.db`
   Docker/Railway создаст ещё два файла: `crab_game.db-wal` и
   `crab_game.db-shm`. Они должны лежать в ТОЙ ЖЕ папке на том же Volume —
   это не мусор, а часть базы (несохранённые последние записи хранятся
   именно в `-wal` до контрольной точки). Если когда-нибудь будешь вручную
   копировать/бэкапить базу — копируй все три файла вместе, иначе можно
   потерять последние записи.

## Админ-команды (боссы/ивенты)

```
/startboss 72 Кракен | Повелитель морей пробудился! Наносите урон, чтобы получить награды.
```
Запускает ивент на 72 часа с именем "Кракен". Игроки видят его в разделе
Прочее → Ивенты и жмут "⚔️ Атаковать босса" — урон суммируется без ограничения
по времени (кулдауна на клик сейчас нет, добавь при необходимости).

```
/endboss
```
Завершает текущий ивент и рассылает награды по местам в топе урона:
- 1 место — 15 раковин, 3 камня 3 уровня, гарантированная особая мутация
- 2-5 место — 7 раковин, 2 камня 2 уровня, 30% шанс особой мутации
- 6-20 место — 2 раковины, 1 камень 1 уровня

## Важные допущения, которые я сделал сам (легко поменять)

Так как не все детали были уточнены, я принял следующие решения — они вынесены
в понятные места кода, чтобы их было легко скорректировать:

- **"Прочность" краба на охоте** (`max_hp` в `game_logic.get_effective_stats`) —
  у краба есть запас HP на одну вылазку, который тратится от атак монстров и
  восстанавливается заново перед каждым новым боем. Без этого механика
  "уклонение" не имела бы смысла. Если враг "убивает" краба — происходит
  отступление на берег и позиция (метры) сбрасывается в 0.
- **Кнопка "Отступить"** позволяет уйти с охоты добровольно, сохранив текущую
  позицию в метрах (чтобы продолжить путь позже).
- **Копание** — сначала жмёшь "Начать копать", через час забираешь добычу
  (3-6 случайных камней). Кулдаун и разброс лежат в `data.py`
  (`DIG_COOLDOWN_SECONDS`).
- **Оплата за реальные деньги** сделана как отдельный подключаемый экран
  (`handlers/profile.py: open_shop`) без реальной интеграции — как только
  оформишь ИП и получишь ключи ЮKassa, там будет пара строк кода на вызов API.
- **Все формулы баланса** (цена уровня краба, цена мутаций, характеристики
  монстров, награда за линьку) собраны **только** в `game_logic.py` — я
  прогнал через них базовые прикидки (см. ниже), но это стартовая точка,
  которую обязательно нужно поднастроить по фидбеку реальных игроков.

## Проверенный баланс (прикидка, не финал)

- Полный путь с 1 до 70 уровня краба при 0 линек ≈ 54 000 золота; после
  3-й линьки та же прокачка обойдётся уже ≈ 120 000 золота — то есть просто
  "накопить и открутить" 70 уровней после нескольких линек становится ощутимо
  дороже.
- Золото за убийство монстра растёт с метрами (~23 на 10 м, ~190 на 100 м,
  ~380 на 200 м) — чем дальше зашёл, тем выгоднее убивать, но и монстры
  сильнее.
- Это **прикидочные цифры без реального плейтеста** — рекомендую погонять бота
  самому/с друзьями хотя бы полчаса и подправить константы в `game_logic.py`
  и `data.py` под то, как реально ощущается темп игры.

## Что стоит доделать дальше

- Добавить небольшой кулдаун на клик "Атаковать босса" в ивентах, иначе можно
  спамить бесконечно (сейчас ограничений по частоте нет).
- Протестировать реальную оплату Stars на живом боте (в тестовом окружении
  Telegram даёт специальный тестовый режим оплаты — см. официальную документацию
  @BotFather → Payments).
- Перейти на персистентное хранилище (Railway Volume — см. выше — уже описано,
  либо PostgreSQL) для продакшена.
- При росте нагрузки — заменить синхронный `sqlite3` на `aiosqlite`.
- Опционально: добавить админ-команду для ручной выдачи уникальной мутации
  конкретному игроку (сейчас они выдаются только через ивенты).
